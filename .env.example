# Root .env (optional) — ใช้ override ค่าใน docker-compose
# คัดลอกเป็น .env แล้วแก้ตามต้องการ

# URL ที่ frontend ใช้เรียก backend
# NEXT_PUBLIC_API_URL=http://localhost:3001

# รายละเอียดเพิ่ม: book-library-backend/.env.example, book-library-frontend/.env.example
